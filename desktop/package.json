{
  "name": "anolis-workbench-desktop",
  "private": true,
  "version": "0.2.0",
  "description": "Tauri desktop wrapper for anolis-workbench",
  "type": "module",
  "scripts": {
    "tauri:dev": "tauri dev --config src-tauri/tauri.conf.json",
    "tauri:build": "tauri build --config src-tauri/tauri.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
