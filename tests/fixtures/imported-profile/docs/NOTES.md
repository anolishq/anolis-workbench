Not part of the imported file set — import copies only machine-profile.yaml,
config/ and behaviors/. Present so the byte-compare tests prove the copy set
is exactly what it claims (see test_import_copies_only_the_declared_set).
