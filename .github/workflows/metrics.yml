name: Metrics

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'  # Weekly Monday 6am UTC
  workflow_dispatch:

permissions: read-all

jobs:
  collect:
    uses: anolishq/.github/.github/workflows/metrics.yml@e993ae623cdaebfaef4e4a25ac806f269a6a2f86 # v2
